exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Tutorial for Hanad the chicken master Ahmed'),
    };
    return response;
};
